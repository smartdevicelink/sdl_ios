language: objective-c
osx_image: beta-xcode6.3
script:
  - xctool test -project SmartDeviceLink-iOS/SmartDeviceLink-iOS.xcodeproj -scheme SmartDeviceLink -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO RUN_CLANG_STATIC_ANALYZER=NO

notifications:
  email: false

before_install:
  - brew update
  - brew outdated xctool || brew upgrade xctool