coverage:
  precision: 2
  round: down

  ignore:
  - Carthage/.*
  - SmartDeviceLinkTests/.*
  - Example Apps/.*
  - bson_c_lib/.*
  status:
    project: false
    patch:
      default:
          enabled: yes
          target: 80%

comment:
  layout: "header, diff, changes, uncovered"
  branches:
    - "!hotfix*"
  behavior: default
